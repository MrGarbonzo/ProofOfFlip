services:
  dashboard:
    image: ghcr.io/mrgarbonzo/proofofflip/dashboard:ff8e40963eddb44df953547c7c32e310f477fb4e
    ports:
      - "3000:3000"
    environment:
      - DASHBOARD_PORT=3000
      - TEE_PROVIDER=secretvm
      - STORAGE_PATH=/data
      - SOLANA_RPC_URL=${SOLANA_RPC_URL}
    volumes:
      - dashboard-data:/data
    restart: unless-stopped

volumes:
  dashboard-data:
