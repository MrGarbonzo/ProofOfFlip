services:
  dashboard:
    image: ghcr.io/mrgarbonzo/proofofflip/dashboard:ee8cf13f3c46bbf843fc560c5434a6895e8c0e7d
    ports:
      - "3000:3000"
    environment:
      - DASHBOARD_PORT=3000
      - TEE_PROVIDER=secretvm
      - STORAGE_PATH=/data
      - SOLANA_RPC_URL=${SOLANA_RPC_URL}
    volumes:
      - dashboard-data:/data
    restart: unless-stopped

volumes:
  dashboard-data:
