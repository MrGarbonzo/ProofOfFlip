services:
  dashboard:
    image: ghcr.io/mrgarbonzo/proofofflip/dashboard:ecef11b0c42af85dcaabbe02016df31d083b8969
    ports:
      - "80:3000"
    environment:
      - DASHBOARD_PORT=3000
      - TEE_PROVIDER=secretvm
      - STORAGE_PATH=/data
      - SOLANA_RPC_URL=${SOLANA_RPC_URL}
    volumes:
      - dashboard-data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  dashboard-data:
